---
id: T-003
title: ilk görev
created_by: user
created: "2026-08-01T03:01:49Z"
updated: "2026-08-01T03:25:37Z"
priority: normal
category: gorev
tags: []
session: S-2026-08-01-b020-mac-kurulum
result: "Çoklu repo desteği uygulandı — repo seçici, yönetim ekranı, repo başına token, repo-farkında outbox. takip@d5c206e; 219 test"
---

# ilk görev

## İstek
birden çok repo eklemek için arayüzü geliştir

## Notlar

- **2026-08-01:** Ele alındı, `id: pending` → `T-003`. Bu, uygulamadan gerçek
  GitHub'a düşen **ilk görev** — B-034'ün cihaz ayağı bu görev üzerinden
  koşuyor. Dosya sözleşmeye uygun geldi: `id: pending`, UTC zaman damgaları,
  `task(pending): inbox'a eklendi (app)` commit öneki.
- **2026-08-01:** İsteğin kapsamı değerlendiriliyor. "Birden çok repo" isteği
  yüzeyde bir arayüz işi gibi görünse de sözleşmenin **hub kökü** tanımına
  (K-012) dokunuyor: uygulama bugün tek bir `<owner>/<repo>` + `hub/` öneki
  varsayıyor; token, ETag önbelleği, yoklama ve outbox'ın tamamı bu tek bağlam
  üzerine kurulu. Kararı kullanıcıya taşımadan uygulamaya başlanmayacak.
- **2026-08-01:** Kullanıcı "şimdi tamamen uygula" dedi; çatal **ayrı repo**
  yönünde çözüldü (istekte açıkça repo geçiyor; B-063'ün tek repoda
  `projects/<ad>/` seçeneği ayrı madde olarak duruyor, bkz. K-018).
- **2026-08-01:** Tamamlandı — takip@d5c206e.

  **Mimari:** `hubConfigProvider` **aktif** bağlantıyı yayınlamayı sürdürüyor;
  çoklu repo onun *altına* bir katman olarak girdi (`hub/hub_connections.dart`).
  İstemci, Contents/Commits/Trees API'leri, yoklama ve görev deposu hiç
  değişmeden çalışıyor — repo değiştirmek onlar için token değiştirmekten
  farksız. Değişikliğin ucuz olmasının nedeni bu darboğazın zaten var olmasıydı.

  **Arayüz:** her ekranın üstünde aktif repoyu gösteren şerit (dokununca repo
  listesi), repo yönetim ekranı (ekle / düzenle / kaldır / geçiş), bağlantı
  ekranında isteğe bağlı ad alanı ve repo başına token.

  **Korunan iki doğruluk noktası:**
  1. Outbox repo-farkında oldu. Taslak kuyruğa alınırken hedef repo damgalanıyor
     ve boşaltmada **her zaman kendi bağlantısına açıkça** gönderiliyor.
     Damgasız olsaydı çevrimdışı yazılan görev, repo değiştikten sonra yanlış
     projeye düşerdi; paylaşılan depoya güvenilseydi hedef,
     `hubConfigProvider`'ın asenkron gecikmesine bağlı kalırdı. İkisi de sessiz
     veri hatası — kullanıcı görevin kaybolduğunu bile fark etmezdi.
  2. Bağlantısı kaldırılan taslak kuyrukta kalır, atılmaz.

  **Yan kazanç:** ETag önbelleği artık repo geçişinde temizlenmiyor (anahtar
  isteğin tam URI'si olduğu için repolar zaten karışamıyor); geçiş anında
  önbellekten çiziliyor.

  **Göç:** T-003 öncesi tek bağlantıyı tutan anahtarlar ilk açılışta listeye
  taşınıyor — kullanıcı token'ı yeniden girmiyor.

  `flutter analyze` temiz, **219 test** geçiyor (28 yeni). Release APK
  derlendi; kullanıcı yeni sürümü henüz cihaza kurmadı.
